[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aifish"
version = "0.1.0"
description = "Initial scaffold for the AIFISH AI phishing detector"
readme = "README.md"
requires-python = ">=3.8"
