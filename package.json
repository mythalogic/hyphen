{
  "name": "hyphen-monorepo",
  "version": "0.1.0",
  "description": "Hyphen - A haiku-based social platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "start": "npm start -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
